# SwiftLintingConfig
Swift linting &amp; formatting configs

In case of SwiftFormat, place the .swiftformat file in someplace, then add the following line to your `zshrc` or `profile`.
```
alias swiftformat="swiftformat --config /path/to/your/dotSwiftFormatFile/"
```
